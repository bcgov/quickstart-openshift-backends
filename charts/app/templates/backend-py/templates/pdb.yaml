{{- if and (index .Values "backend-py").pdb  (index .Values "backend-py").pdb.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "backend-py.fullname" . }}
  labels:
    {{- include "backend-py.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "backend-py.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  minAvailable: {{ (index .Values "backend-py").pdb.minAvailable }}
{{- end }}
